{
  "name": "platepe-pos",
  "private": true,
  "version": "1.0.0",
  "description": "PlatePe POS — monorepo root. Frontend lives in app/, backend lives in backend/.",
  "scripts": {
    "dev:app": "cd app && npm run dev",
    "dev:backend": "cd backend && npm run start:dev",
    "install:all": "cd app && npm install && cd ../backend && npm install"
  }
}
